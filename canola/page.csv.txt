average_rating,average_tag: mean ueber alle nodes (tag vs. rating wie gehabt)
diversity_rating:
diversity_tag: in der abfolge der nodes die summe der differenzen aufeinderfolgenden nodes
num_neg_bias, num_pos_bias: analog zu user.csv, aber auf doks bezogen
    haben die user insgesamt tendenziell eher pos oder neg 'daneben' gelegen,
    auf alle node_ids im dokument bezogen
{
    raw_page_domnodes
    raw_page_userids
    raw_tag_1,
    raw_tag_2
    raw_tag_3
    raw_tag_none
    raw_tag_null
    raw_tag_undefined
}: unbereinigte counts fuer die vergebenen tags (insbes: inkl. null, undefined, etc.)

sum_bias: summe der biase der nodes, d.h. tendenz der user auf dieser seite (mit extremen heben sich auf)
sum_difficulty: sum( delta(tag, rating)), d.h. hoher wert: uneinig

{
sum_neg_bias
sum_pos_bias
}: wie in user, siehe oben: num_neg_bias, etc...

tag_by_average_rating: (int!) rounded mean of ratings, d.h. sum(ratings_der_nodes)/anzahl(nodes)
tag_by_average_tag: (int!) rounded mean of tags, d.h. sum(tag_der_nodes)/anzahl(nodes)

{
withmerge_page_domnodes
withmerge_page_userids
withmerge_tag_1
withmerge_tag_2
withmerge_tag_3
withmerge_tag_none
}: wie raw, aber nach bereinigung (wie von egon gewuenscht)
