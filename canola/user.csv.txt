userid: user_id
{
    1
    2
    3
}: anzahl der tags, die user_id getagged hat
bias_rating: (float) wie sehr lag der user aufsummiert 'daneben', 0-zentriert, d.h. 
    er kann genau so weit pos. wie neg. daneben gelegen haben und kommt bei 0
    raus
bias_tag wie rating, aber bezogen aufs tag (d.h. int)
count_tag: sum(1+2+3)
differ_rating: >=0 (ist float), wie differ_tag, aber nicht gerunded, d.h.
    mean(sum( tag(node,user_id) for user_id in userids_of_node if tag(node,user_id!=none)) 
    note: bloede tagger verschlechtern 'mein' ergebnis - das ist nicht toll!
differ_tag: >=0 (ist int), summe(all:delta(node_tag,  aggreed_tag))
    (mit agreed_tag ist int,gerundet; d.h. 1.49 -> 1; 1.51 -> 2, etc)
difficulty_rating: summe ueber alle node_id des users, delta(tag,rating) 
none: wie 1,2,3,

gen.note: alle werte sollten via count_tag normalisiert werden
